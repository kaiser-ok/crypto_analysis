# 虛擬貨幣地址比對與修正功能說明

## 概述

本專案針對台灣法院詐欺案判決書中出現的 TRON (TRC-20) 虛擬貨幣地址，提供完整的**擷取 → 驗證 → 修正 → 鏈上確認 → 風險分析**管線，處理判決書文本中因 OCR 掃描、人工繕打等原因產生的地址錯誤。

---

## 一、地址擷取（extractor.py）

### 正規表達式策略

採用**寬鬆模式**擷取，故意容許可能含錯的地址進入後續修正流程：

| 模式 | 正規表達式 | 說明 |
|------|-----------|------|
| 嚴格模式 | `T[Base58]{33}` | 僅允許合法 Base58 字元（不含 `0`, `O`, `I`, `l`） |
| 寬鬆模式 | `T[A-Za-z0-9]{32,34}` | 容許長度 33\~35 字元、包含非 Base58 字元 |

擷取時使用寬鬆模式，確保 OCR 產生的 `0`（零）、`O`（大寫 O）、`I`（大寫 I）、`l`（小寫 L）等常見混淆字元不會遺漏。

### Base58 字元集

```
123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz
```

注意：排除 `0`（零）、`O`（大寫 O）、`I`（大寫 I）、`l`（小寫 L）以避免視覺混淆。

### 擷取流程

1. 對每份判決書全文以寬鬆正則掃描所有候選地址
2. 記錄前後各 40 字元的**上下文**（context），供後續人工比對
3. 以 `(address, case_id)` 為鍵去重，避免同案同地址重複計入
4. 輸出 `ExtractedAddress` 物件，含 `address`、`case_id`、`court`、`context`

---

## 二、Base58Check 驗證（validator.py）

### 驗證規則

對每個擷取出的地址依序檢查：

| 順序 | 檢查項目 | 錯誤類型 | 說明 |
|------|---------|----------|------|
| 1 | 長度 | `LENGTH`（長度錯誤） | 必須恰好 34 字元 |
| 2 | 前綴 | `INVALID_CHARS`（含非 Base58 字元） | 必須以 `T` 開頭 |
| 3 | 字元集 | `INVALID_CHARS`（含非 Base58 字元） | 所有字元必須在 Base58 字元集內 |
| 4 | Checksum | `CHECKSUM`（Checksum 錯誤） | `base58.b58decode_check()` 驗證通過 |
| 5 | 版本位元組 | `VERSION_BYTE`（Version byte 錯誤） | 解碼後 21 bytes、首位元組為 `0x41` |

全部通過則為 `NONE`（無錯誤），地址有效。

### 修正策略

對驗證失敗的地址，依序嘗試三種修正策略：

#### 策略 1：常見字元替換

針對 OCR/繕打最常混淆的字元進行映射替換：

| 原始字元 | 替換候選 | 混淆原因 |
|---------|---------|---------|
| `0`（零） | `o`, `D` | 零與小寫 o、大寫 D 外形相似 |
| `O`（大寫 O） | `o` | 大小寫混淆 |
| `I`（大寫 I） | `1`, `i` | 大寫 I 與數字 1、小寫 i 相似 |
| `l`（小寫 L） | `1`, `L` | 小寫 l 與數字 1、大寫 L 相似 |

對地址中所有非 Base58 字元位置，遞迴組合所有替換候選，逐一驗證 Base58Check。

#### 策略 2：單字元暴力搜尋

對 34 字元地址的每個位置，嘗試替換為所有 58 個 Base58 字元（排除原字元），驗證是否產生合法地址。此策略可修正任意單一字元錯誤。

#### 策略 3：相鄰字元對調（Transposition）

嘗試交換相鄰兩個字元的位置，涵蓋 OCR 掃描中常見的字元順序顛倒錯誤。

三種策略的結果取聯集，排序後回傳為 `suggestions` 列表。

---

## 三、OCR 進階驗證（report.py → tron-ocr-verify API）

### 外部依賴

呼叫 `localhost:3000/api/v1/verify`（tron-ocr-verify Node.js 服務），以 POST 方式送出待驗證地址。

### API 回傳結構

```json
{
  "checksumValid": false,
  "bestMatch": "T...(修正後地址)",
  "overallConfidence": 0.95,
  "verified": [
    {
      "address": "T...",
      "corrections": [
        { "position": 33, "from": "L", "to": "1", "type": "substitution" }
      ],
      "accountInfo": { "isActive": true, "balance": 1000000 }
    }
  ],
  "unverified": [...]
}
```

### 關鍵欄位

| 欄位 | 說明 |
|------|------|
| `checksumValid` | 原始地址是否直接通過 Base58Check |
| `bestMatch` | 最佳修正地址（可能與原始相同） |
| `overallConfidence` | 修正信心分數 (0.0 ~ 1.0) |
| `verified` | 已上鏈驗證的候選地址列表 |
| `unverified` | 候選但未在鏈上找到的地址列表 |
| `corrections` | 每處修正的位置、原字元、修正字元、修正類型 |

### 修正類型

| 類型 | 說明 | 顯示範例 |
|------|------|---------|
| `substitution` | 字元替換 | 位置 33: `L` → `1` |
| `deletion` | 多餘字元刪除 | 位置 12: 刪除 `O` |
| `insertion` | 缺少字元插入 | 位置 8: 插入 `k` |

---

## 四、MistTrack 風險分析

對驗證通過或修正成功的地址，呼叫 MistTrack API 進行 AML 風險分析：

- 僅對有效（或已修正）地址進行分析
- 地址去重後批次查詢，結果快取避免重複呼叫
- 分析項目：風險分數/等級、標籤、交易統計、惡意事件、使用平台、資金來源

---

## 五、UI 顯示與互動（report.py 生成之 HTML 報告）

### 狀態分類

| 狀態 | CSS 類別 | 顏色 | 判定條件 |
|------|---------|------|---------|
| 有效 | `status-valid` | 綠色 | `checksumValid == true` |
| 已修正 | `status-corrected` | 黃色 | 原始無效但 `bestMatch` 存在 |
| 無效 | `status-invalid` | 紅色 | 原始無效且無法修正 |

### 信心分數標籤

| 等級 | 範圍 | CSS 類別 | 顏色 |
|------|------|---------|------|
| 高 | ≥ 80% | `badge-high` | 綠色 |
| 中 | 50% ~ 79% | `badge-mid` | 黃色 |
| 低 | < 50% | `badge-low` | 紅色 |

### 統計卡片

報告頂部顯示以下統計數據：

- **地址總數** — 擷取出的全部候選地址
- **格式有效** — 直接通過 Base58Check 驗證
- **格式無效** — 驗證未通過
- **成功修正** — 原始無效但已找到修正地址
- **鏈上驗證** — 修正地址在 TRON 鏈上確認存在
- **風險分析** — 已完成 MistTrack 分析的地址數
- **中/高風險** — 風險等級分布
- **涉及惡意活動** — 涉及釣魚、勒索、盜竊、洗錢的地址數

### 篩選功能

支援按狀態篩選（全部/有效/已修正/無效/涉及惡意）及關鍵字搜尋。涉及惡意活動的列以紅色左邊框標記。

---

## 六、修正地址回饋（案情分析整合）

### 前端收集

JavaScript 函式 `collectCorrectedAddrs(caseId)` 從表格 DOM 收集該案件所有地址的修正對應：

```javascript
// 結果格式：{ "原始錯誤地址": "修正後地址", "正確地址": "正確地址" }
```

### 後端套用（case_analyzer.py）

案情分析 API (`/api/case-analysis`) 接收 `address_corrections` 字典，在建立錢包清單時優先使用修正後地址：

```python
corrected_addr = corrections.get(raw_addr, raw_addr)
```

確保後續幣流追蹤、交易所報告等功能使用的是**驗證後的正確地址**，而非判決書中可能含錯的原始字串。

---

## 七、完整管線流程圖

```
判決書全文
    │
    ▼
┌──────────────────┐
│ 1. 寬鬆正則擷取     │  extractor.py
│   T[A-Za-z0-9]{32,34} │  容許 OCR 錯字
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 2. Base58Check 驗證  │  validator.py
│   長度/字元/Checksum │
│   + 三策略自動修正    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 3. OCR 進階驗證      │  tron-ocr-verify API
│   ML 修正 + 信心分數  │  (localhost:3000)
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 4. 鏈上驗證         │  TronGrid / Tronscan
│   確認地址是否存在    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 5. MistTrack 風險分析 │  MistTrack API
│   AML 風險/標籤/惡意  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 6. HTML 互動報告     │  report.py
│   狀態/修正/信心/風險  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 7. 修正地址回饋      │  case_analyzer.py
│   案情分析/幣流追蹤   │  使用修正後地址
└──────────────────┘
```
